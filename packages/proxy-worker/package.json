{
  "name": "@edu-role-play/proxy-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that fronts default edu-role-play bundles. Calls Workers AI via the env.AI binding (no API keys), adds CORS, and rate-limits per IP.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.80.0"
  }
}
